# ==============================================
# Переменные окружения для примеров observability
# ==============================================

# 📊 LangSmith - Трассировка и мониторинг (example1_langsmith_tracing.py)
# Получите ключи на https://smith.langchain.com
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=your-api-key-here
LANGSMITH_PROJECT=pr-artistic-gastropod-72

# Для обратной совместимости (legacy)
LANGCHAIN_API_KEY=your-api-key-here
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=pr-artistic-gastropod-72

# 🔍 LangFuse - Open Source мониторинг (example3_langfuse_monitoring.py)
# Для self-hosted развертывания через Docker
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# 🤖 OpenAI API - для чата с LLM (example3e_openai_chat.py)
# Получите ключ на https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OPENAI_ORGANIZATION=org-your-organization-id  # Опционально (если нужно)

# 🧠 Anthropic API - Для моделей Claude (example4_litellm_cost_control.py)
# Получите ключ на https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# 🌟 Google AI - Для моделей Gemini (опционально для LiteLLM)
# Получите ключ на https://ai.google.dev
GOOGLE_API_KEY=your-google-ai-api-key-here

# 🚀 Azure OpenAI - Альтернатива OpenAI (опционально)
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2023-12-01-preview

# 💾 База данных для хранения метрик (опционально)
DATABASE_URL=postgresql://user:password@localhost:5432/observability_db

# 🔧 Настройки приложения
# Окружение (development, staging, production)
ENVIRONMENT=development

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Порт для веб-интерфейсов (если используется Streamlit)
STREAMLIT_PORT=8501

# 📈 Настройки бюджета для LiteLLM
# Максимальный дневной бюджет в долларах
DAILY_BUDGET=10.0

# Максимальная стоимость одного запроса в долларах
MAX_REQUEST_COST=1.0

# 🎯 Настройки DSPy (example2_dspy_optimization.py)
# Модель по умолчанию для DSPy экспериментов
DSPY_MODEL=gpt-3.5-turbo

# Максимальное количество примеров для обучения
DSPY_MAX_EXAMPLES=50

# 🔐 Безопасность
# Секретный ключ для подписи сессий
SECRET_KEY=your-secret-key-for-sessions

# Разрешенные хосты (через запятую)
ALLOWED_HOSTS=localhost,127.0.0.1

# 📊 Мониторинг и алерты
# Webhook URL для уведомлений о превышении бюджета
BUDGET_ALERT_WEBHOOK=https://hooks.slack.com/your-webhook-url

# Email для критических уведомлений
ALERT_EMAIL=admin@yourcompany.com

# 🐳 Docker настройки для LangFuse v3
# Пароль для PostgreSQL в Docker Compose
POSTGRES_PASSWORD=langfuse_secure_password

# Пароль для ClickHouse (новое требование v3)
CLICKHOUSE_PASSWORD=langfuse_clickhouse_password

# Секретный ключ для NextAuth в LangFuse
NEXTAUTH_SECRET=your-nextauth-secret-key

# URL для аутентификации в LangFuse
NEXTAUTH_URL=http://localhost:3000

# SALT для шифрования API ключей (минимум 32 символа)
SALT=langfuse-super-secret-salt-key-32-chars

# ==============================================
# Примеры использования:
# ==============================================

# 1. Для быстрого старта с LangSmith:
#    - Зарегистрируйтесь на https://smith.langchain.com
#    - Создайте API ключ
#    - Установите LANGCHAIN_API_KEY
#    - Запустите: python example1_langsmith_tracing.py

# 2. Для self-hosted LangFuse:
#    - Запустите: docker-compose up -d (файл в документации)
#    - Откройте http://localhost:3000
#    - Создайте проект и получите ключи
#    - Установите LANGFUSE_* переменные

# 3. Для DSPy оптимизации:
#    - Установите OPENAI_API_KEY
#    - Настройте DSPY_MODEL
#    - Запустите: python example2_dspy_optimization.py

# 4. Для контроля затрат с LiteLLM:
#    - Установите API ключи провайдеров
#    - Настройте DAILY_BUDGET
#    - Запустите: python example4_litellm_cost_control.py

# ==============================================
# Полезные команды:
# ==============================================

# Загрузить переменные в текущую сессию:
# export $(cat .env | xargs)

# Проверить подключение к LangSmith:
# python -c "from langsmith import Client; print(Client().list_runs(limit=1))"

# Запустить LangFuse локально:
# docker run -d -p 3000:3000 langfuse/langfuse:latest

# Проверить использование API OpenAI:
# curl -H "Authorization: Bearer $OPENAI_API_KEY" https://api.openai.com/v1/usage
